1 - ¿Porque es importante validar los datos que ingresa un usuario?
```python
Es importante porque, si el usuario ingresa datos erráticos o que no estan contemplados dentro del programa, puede hacer que éste se rompa o que su funcionalidad sea desprolija y/o incorrecta.
```
2 - ¿Que hace strip y en que se diferencia de split?
```python
.strip() es una función que llamamos para limpiar el primer y el último caracter de un string. En cambio, .split() es una función que usamos para separar y extraer información de un texto de manera prolija.
```
3 - ¿Para que puede servir count cuando una linea tiene datos separados por comas o por punto y coma?
```python
Puede servir para contar cuantos elementos o variables tiene una lista. Por ejemplo, si tiene dos comas, quiere decir que hay 3 elementos.
```
4 - ¿Que problema pueden traer los espacios de mas al principio o al final de un dato?
```python
El programa va a interpretar ese espacio como un caracter más y, si recorro letras o cantidad de elementos, puede ser que me arroje resultados incorrectos o no deseados.
```
